// ignore_for_file: public_member_api_docs

enum RequestMethod {
  get,
  post,
  put,
}

enum Option {
  one,
  two,
  three,
}

enum Type {
  student,
  supervisor,
}

enum MessageDeliveryStatus {
  sending,
  failed,
  sent,
}
